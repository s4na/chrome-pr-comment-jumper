{
  "name": "chrome-pr-comment-jumper",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Chrome extension to jump between PR comments on GitHub",
  "scripts": {
    "test": "jest",
    "build": "rm -rf dist && mkdir -p dist/icons && cp manifest.json content.js content.css dist/ && cp icons/* dist/icons/"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
